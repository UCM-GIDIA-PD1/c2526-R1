[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."] # Le dice a Hatch que el código está en esta misma carpeta

[project]
name = "comun"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []